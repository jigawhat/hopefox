{
  "name": "hopefox-netlify",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "netlify dev",
    "build": "echo 'No build step required'",
    "start": "netlify dev"
  },
  "dependencies": {
    "@netlify/functions": "^3.1.0"
  },
  "devDependencies": {
    "netlify-cli": "^23.2.0"
  }
}
